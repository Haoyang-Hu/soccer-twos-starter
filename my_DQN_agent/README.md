# My DQN Agent

**Agent name:** MyDQN

## Description

An agent using a DQN policy trained with `example_ray_dqn_sp.py` on the `team_vs_policy` variation with `single_player=True` and `flatten_branched=True`. Loads weights from `ray_results/DQN_1/`.
